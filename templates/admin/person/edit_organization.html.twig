{% extends 'admin/layout.html.twig' %}

{% block title %}Editar vínculo institucional{% endblock %}

{% block main %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-1">Editar vínculo institucional</h1>
            <p class="text-body-secondary mb-0">Atualize os dados deste vínculo institucional.</p>
        </div>
        <a class="btn btn-outline-secondary" href="{{ path('admin_person_show', {'id': personOrganization.person.id}) }}">Voltar</a>
    </div>

    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
                <div class="row g-3">
                    <div class="col-md-6">{{ form_row(form.startDate) }}</div>
                    <div class="col-md-6">{{ form_row(form.endDate) }}</div>
                    <div class="col-md-6">{{ form_row(form.status) }}</div>
                    <div class="col-12">{{ form_row(form.notes) }}</div>
                </div>

                <div class="mt-4 d-flex gap-2">
                    <button class="btn btn-primary">Salvar alterações</button>
                    <a class="btn btn-outline-secondary" href="{{ path('admin_person_show', {'id': personOrganization.person.id}) }}">Cancelar</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
